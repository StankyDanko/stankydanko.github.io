import React, { useState } from "react";
import { motion, AnimatePresence } from "framer-motion";

// --- Framer Motion Variants ---
const containerVariants = {
  hidden: { opacity: 0 },
  show: {
    opacity: 1,
    transition: { staggerChildren: 0.2, delayChildren: 0.3 }
  }
};
const itemVariants = {
  hidden: { opacity: 0, y: 50 },
  show: { opacity: 1, y: 0, transition: { type: "spring", stiffness: 100 } }
};
const pulseVariants = {
  breathe: {
    scale: [1, 1.02, 1],
    filter: [
      "drop-shadow(0px 0px 10px rgba(16,185,129,0.4))",
      "drop-shadow(0px 0px 25px rgba(16,185,129,0.8))",
      "drop-shadow(0px 0px 10px rgba(16,185,129,0.4))"
    ],
    transition: { duration: 4, repeat: Infinity, ease: "easeInOut" }
  }
};

export default function StankyHub() {
  const [activeNode, setActiveNode] = useState(null);
  const links = [
    { name: "GitHub", url: "https://github.com/StankyDanko", color: "hover:shadow-[#333]" },
    { name: "X (Twitter)", url: "https://x.com/stankydanko", color: "hover:shadow-[#1DA1F2]" },
    { name: "YouTube", url: "https://www.youtube.com/@StankyDanko", color: "hover:shadow-[#FF0000]" }
  ];

  const navItems = [
    { label: "Home", href: "#hero" },
    { label: "About", href: "#about" },
    { label: "Portfolio", href: "#portfolio" },
    { label: "Contact", href: "#contact" }
  ];

  return (
    <div className="min-h-screen bg-zinc-950 text-emerald-50 font-sans overflow-hidden relative flex flex-col">
      {/* Nav Component */}
      <motion.nav
        variants={containerVariants}
        initial="hidden"
        animate="show"
        className="fixed top-0 w-full z-20 bg-gradient-to-r from-emerald-800 to-purple-900 shadow-stanky py-4"
      >
        <div className="flex justify-center gap-6">
          {navItems.map((item) => (
            <motion.a
              key={item.label}
              href={item.href}
              whileHover={{ y: -2, scale: 1.05 }}
              className="text-emerald-100 font-bold"
            >
              {item.label}
            </motion.a>
          ))}
        </div>
      </motion.nav>

      {/* Hero Section */}
      <section id="hero" className="flex flex-col items-center justify-center h-screen relative">
        {/* Dynamic Background Mesh */}
        <div className="absolute inset-0 z-0 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-emerald-900/20 via-zinc-950/80 to-zinc-950" />

        <motion.div
          variants={containerVariants}
          initial="hidden"
          animate="show"
          className="relative z-10 flex flex-col items-center w-full max-w-4xl"
        >
          {/* Hero Mascot SVG */}
          <motion.svg
            viewBox="0 0 300 300"
            className="w-64 h-64 md:w-80 md:h-80 mb-8 cursor-pointer"
            variants={pulseVariants}
            animate="breathe"
            whileHover={{ scale: 1.05, rotate: [0, -2, 2, 0] }}
            transition={{ type: "spring", stiffness: 300 }}
            whileTap={{ scale: 0.95 }}
          >
            {/* Leaf Body Group */}
            <motion.g variants={itemVariants}>
              <motion.path
                d="M150 250 Q150 200 150 150 L100 100 Q120 80 150 100 L200 100 Q180 80 150 100"
                fill="#10B981" // stankyGreen
                stroke="#A5F3FC" // lime-300 equivalent
                strokeWidth={2}
                initial={{ pathLength: 0 }}
                animate={{ pathLength: 1 }}
                transition={{ duration: 1.5, ease: "easeInOut" }}
              />
            </motion.g>

            {/* Goggle Eyes Group */}
            <motion.g variants={pulseVariants} animate="breathe">
              <motion.circle
                cx={130}
                cy={120}
                r={15}
                fill="white"
                stroke="black"
                strokeWidth={3}
                whileHover={{ scale: 1.1, y: -3 }}
                transition={{ yoyo: Infinity, duration: 0.4 }}
                whileTap={{ scaleY: 0.1, transition: { duration: 0.2 } }} // Blink
              />
              <motion.circle
                cx={170}
                cy={120}
                r={15}
                fill="white"
                stroke="black"
                strokeWidth={3}
                whileHover={{ scale: 1.1, y: -3 }}
                transition={{ yoyo: Infinity, duration: 0.4 }}
                whileTap={{ scaleY: 0.1, transition: { duration: 0.2 } }} // Blink
              />
            </motion.g>

            {/* Grin Mouth */}
            <motion.path
              d="M120 180 Q150 200 180 180"
              stroke="black"
              strokeWidth={5}
              fill="none"
              initial={{ pathLength: 0 }}
              animate={{ pathLength: 1 }}
              transition={{ duration: 1.2, ease: "easeOut" }}
              whileHover={{ d: "M110 180 Q150 210 190 180" }} // Wider grin
            />

            {/* Georgia Overlay */}
            <motion.path
              d="M140 220 Q160 240 180 220 M150 230 L150 250"
              stroke="#6D28D9" // dankPurple
              strokeDasharray={5}
              strokeWidth={3}
              fill="none"
              opacity={0.7}
              initial={{ strokeDashoffset: 50 }}
              animate={{ strokeDashoffset: 0 }}
              transition={{ delay: 1, duration: 1.5 }}
            />

            {/* Smoke Puffs Group (on tap) */}
            <motion.g
              initial={{ opacity: 0 }}
              whileTap={{ opacity: 1, transition: { staggerChildren: 0.1 } }}
            >
              <motion.path
                d="M150 200 Q170 180 190 200"
                stroke="#10B981" // stankyGreen /50
                strokeOpacity={0.5}
                strokeWidth={4}
                fill="none"
                animate={{ y: [0, -50], opacity: [1, 0], scale: [1, 1.5] }}
                transition={{ duration: 2 }}
              />
              {/* Add 2-4 more similar paths offset for full puff effect */}
            </motion.g>
          </motion.svg>

          {/* Brand Header */}
          <motion.div variants={itemVariants} className="text-center mb-12">
            <h1 className="text-5xl md:text-7xl font-black text-transparent bg-clip-text bg-gradient-to-r from-emerald-400 to-lime-300 tracking-tighter uppercase mb-2">
              Stanky Danko
            </h1>
            <p className="text-emerald-400/70 text-lg md:text-xl font-mono tracking-widest">
              // High-Tech Botany & Code //
            </p>
          </motion.div>

          {/* Interactive Social Nodes */}
          <motion.div variants={itemVariants} className="flex flex-wrap justify-center gap-6 w-full">
            {links.map((link) => (
              <motion.a
                key={link.name}
                href={link.url}
                target="_blank"
                rel="noopener noreferrer"
                onMouseEnter={() => setActiveNode(link.name)}
                onMouseLeave={() => setActiveNode(null)}
                whileHover={{ y: -8, scale: 1.05 }}
                whileTap={{ scale: 0.95 }}
                className={`relative px-8 py-4 rounded-2xl bg-zinc-900/50 border border-emerald-500/20 backdrop-blur-sm transition-all duration-300 ${link.color}`}
              >
                {/* Inline SVG Icon Example (Arrow) */}
                <motion.svg
                  viewBox="0 0 24 24"
                  className="w-5 h-5 inline-block mr-2"
                  whileHover={{ rotate: 90 }}
                  transition={{ duration: 0.3 }}
                >
                  <motion.path
                    d="M12 0 L24 12 L12 24"
                    fill="none"
                    stroke="#D1FAE5" // emerald-100
                    strokeWidth={2}
                  />
                </motion.svg>
                <span className="relative z-10 font-bold tracking-wide text-emerald-100">
                  {link.name}
                </span>

                {/* Active Hover Glow Indicator */}
                <AnimatePresence>
                  {activeNode === link.name && (
                    <motion.div
                      initial={{ opacity: 0, scale: 0.8 }}
                      animate={{ opacity: 1, scale: 1 }}
                      exit={{ opacity: 0, scale: 0.8 }}
                      className="absolute inset-0 border-2 border-emerald-400 rounded-2xl z-0"
                      layoutId="glow"
                    />
                  )}
                </AnimatePresence>
              </motion.a>
            ))}
          </motion.div>
        </motion.div>
      </section>

      {/* About Section */}
      <section id="about" className="p-8 bg-zinc-900/50 backdrop-blur-md">
        <motion.h2 variants={itemVariants} className="text-3xl font-bold mb-4 text-center">
          About Stanky Danko
        </motion.h2>
        <p className="text-center max-w-2xl mx-auto">
          From Georgia pines to pixel haze—Danko's stanky journey in tech & art.
        </p>
        {/* About Timeline SVG */}
        <motion.svg
          viewBox="0 0 800 100"
          className="w-full h-24 mt-8"
          initial={{ pathLength: 0 }}
          whileInView={{ pathLength: 1 }}
          transition={{ duration: 2 }}
          viewport={{ once: true }}
        >
          {/* Base Line */}
          <motion.path
            d="M0 50 H800"
            strokeDasharray="5 5"
            stroke="#10B981"
            strokeOpacity={0.5}
            strokeWidth={3}
            fill="none"
          />
          {/* Milestone Example (Add more as needed) */}
          <motion.g whileHover={{ scale: 1.3 }} variants={itemVariants}>
            <motion.circle cx={100} cy={50} r={10} fill="#6D28D9" />
            <motion.text x={100} y={70} fill="#D1FAE5" fontFamily="monospace" textAnchor="middle">
              Born in Barnesville
            </motion.text>
          </motion.g>
          {/* Repeat for other milestones at cx=250, etc. */}
        </motion.svg>
      </section>

      {/* Portfolio Section */}
      <section id="portfolio" className="p-8 grid grid-cols-1 md:grid-cols-3 gap-6">
        {/* Example Portfolio Card (Repeat for multiple) */}
        <motion.div
          className="bg-zinc-900/50 p-4 rounded-2xl"
          variants={itemVariants}
          whileInView="show"
          initial="hidden"
          viewport={{ once: true }}
        >
          <motion.svg
            viewBox="0 0 100 100"
            className="w-16 h-16 mx-auto mb-4"
            whileHover={{ rotate: [0, 3, -3, 0] }}
            transition={{ duration: 0.4 }}
          >
            {/* Face */}
            <motion.circle cx={50} cy={50} r={40} fill="#A5F3FC" variants={pulseVariants} animate="breathe" />
            {/* Add eyes, mouth, haze as per YAML */}
          </motion.svg>
          <p className="text-center">Dank Meme Example</p>
        </motion.div>
        {/* Add more cards */}
      </section>

      {/* Contact Section */}
      <section id="contact" className="p-8 max-w-md mx-auto bg-gradient-to-b from-purple-900 to-emerald-800 rounded-2xl">
        <motion.h2 variants={itemVariants} className="text-3xl font-bold mb-4 text-center">
          Get in Touch
        </motion.h2>
        <form className="flex flex-col gap-4">
          <input type="email" placeholder="Email" className="p-2 rounded bg-zinc-800 text-emerald-50" />
          <textarea placeholder="Message" className="p-2 rounded bg-zinc-800 text-emerald-50" />
          <motion.button
            type="submit"
            className="px-4 py-2 bg-emerald-600 rounded"
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
          >
            Submit
            {/* Loader SVG (Show conditionally on submit) */}
            {/* <motion.svg viewBox="0 0 50 50" className="w-6 h-6 inline-block ml-2">
              <motion.path d="M25 25 C35 15 45 25 35 35 C25 45 15 35 25 25" fill="#10B981"
                animate={{ rotate: 360 }} transition={{ repeat: Infinity, duration: 1.5 }} />
            </motion.svg> */}
          </motion.button>
        </form>
        {/* Duplicate social nodes here if desired */}
      </section>

      {/* Footer */}
      <footer className="text-center py-4 bg-zinc-950 text-emerald-400/70">
        © 2026 StankyDanko – Keep It Dank, Barnesville
      </footer>
    </div>
  );
}